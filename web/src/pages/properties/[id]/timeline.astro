---

import PropertyLayout from "../property-layout.astro";
import PropertyTimeline from "../../../components/properties/PropertyTimeline";
const { id } = Astro.params
---

<PropertyLayout>
    <PropertyTimeline id={id} client:only="solid-js"/>
</PropertyLayout>
